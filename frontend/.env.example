# Cible du proxy Vite (dev + preview) — doit être l’URL du serveur Express (voir PORT dans backend/.env)
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5000

# Déploiement : URL publique complète du backend si le frontend est servi ailleurs (pas de proxy)
# VITE_API_URL=https://votre-api.exemple.com
